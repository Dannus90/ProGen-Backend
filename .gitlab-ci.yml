name: CI-Tests

image: microsoft/dotnet:latest

stages:
  - build

variables:
  project: "ProGenBackend"

before_script:
  - "dotnet restore"

build:
  stage: build
  variables:
    build_path: "$project"
  script:
    - "dotnet build"
